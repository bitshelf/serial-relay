# Cross-compilation linker override (only used on non-native targets).
# When building natively on ARM64, the system linker is used.
[target.aarch64-unknown-linux-gnu]
# linker = "rust-lld"  # Uncomment for x86->ARM64 cross-compilation
